# AGENTS.md

## 范围
- 本文件适用于仓库根目录下的所有代码与文档。

## 通用要求
- 始终使用简体中文回复。
- 先读后改：修改前先用 `rg`/`sed`/`cat` 查看相关文件。
- 优先使用 `rg` 进行搜索；找文件用 `rg --files`。
- 默认使用 ASCII 编辑，除非文件已有非 ASCII 且确有必要。
- 尽量用 `apply_patch` 做单文件小改；批量改动用脚本。
- 不要删除或回滚你没有创建的改动，除非用户明确要求。
- 发现意外更改时立刻停下并询问用户。

## 代码与测试
- 只在需要时添加简洁注释，避免无意义注释。
- 改动要最小化，遵循现有风格。
- 若无法运行测试，说明原因并给出建议的验证命令。

## 输出格式
- 最终答复简洁、结构清晰，优先给出结果与下一步建议。
